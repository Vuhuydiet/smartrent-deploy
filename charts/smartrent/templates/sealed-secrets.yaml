{{- if .Values.sealedSecrets.enabled }}
# This template will include the sealed secrets from the environment-specific directory
# Sealed secrets are environment-specific and stored as separate files
# Include the sealed-secrets.yaml file from the environments directory
{{- end }}
